Resolved. Cause: GPU/display driver regression introduced by the overnight POOL-FIN-01 image update, causing dwm.exe crashes in igdumd64.dll and session disconnects. Action: Contained affected hosts, drained sessions, reverted hosts to a known-good image/driver baseline, restarted hosts, and validated stable logins by 10:00 AM with no further reports. Preventive: Pin validated display driver in the image baseline, block unvalidated driver drift, and enforce staged rollout with event-based halt criteria (Event 1000 dwm.exe+igdumd64.dll and Event 21 followed by Event 40/9009). User confirmed working.
